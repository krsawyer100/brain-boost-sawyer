const auth = require("./auth");
const user = require("./users");
const set = require("./studySets")
const flashcard = require("./flashcards")

module.exports = {
  auth,
  user,
  set, 
  flashcard,
};
